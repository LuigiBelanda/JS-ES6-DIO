function soma(a, b) {
    return a + b;
}

soma(2, 2);
soma(2, 3);
soma(2, 4);
soma(2, 5);

// Neste caso, como o primeiro argumento sempre se repete, poderiamos fazer currying
// como no outro exemplo